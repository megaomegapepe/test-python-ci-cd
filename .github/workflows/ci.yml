name: Run tests on any Push event into develop branch
# Запуск при любом push евенте в любой ветке, за исключением релизных тэгов.
# Они будт тестироваться перед сборкой
on:
  push:
    tags:
      - '!refs/tags/*'
    branches:
      - '*'
jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
      - name: echo
        run: cd /home/runner/work/test-python-ci-cd/test-python-ci-cd/
      # Устанавливаем python нужной версии
      - uses: actions/setup-python@v1
        with:
          python-version: '3.8'
          architecture: 'x64'
      - name: Run tests
        run: ./home/runner/work/test-python-ci-cd/test-python-ci-cd/tests.py